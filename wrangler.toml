name = "xiv-party-builder"
compatibility_date = "2025-01-01"
pages_build_output_dir = "."

# Le binding KV PARTY_KV est défini côté dashboard Cloudflare Pages :
#   Settings → Functions → KV namespace bindings
#
# Pour le dev local :
#   wrangler pages dev . --kv PARTY_KV
# (crée un KV éphémère Miniflare ; les données ne touchent pas la prod)
